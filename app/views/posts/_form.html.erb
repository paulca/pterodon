<%= form_with(model: post, data: { controller: "persist-input", persist_input_key_value: "post_form_content" }) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content, data: { persist_input_target: "textarea", controller: "textarea-autogrow" } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
